---
artifact: spec
loop: 3
project: issue-3
backend: claude(opus)
role: planner
created_at: 2026-03-05T04:40:55Z
---

# Feature: Repository Hygiene — Remove Tracked Runtime Artifact

## Description
Remove the accidentally tracked `state.sqlite` binary runtime artifact from git history on this branch and add it to `.gitignore` to prevent recurrence. This is the sole remaining blocker identified by acceptance QA — all functional requirements (crypto signing, key parsing, tests) are already implemented and passing.

## Acceptance Criteria
- [ ] `state.sqlite` is removed from git tracking on this branch (`git rm state.sqlite`).
- [ ] `*.sqlite` (or at minimum `state.sqlite`) is added to `.gitignore` at the repository root.
- [ ] `git diff master...HEAD --stat` no longer includes `state.sqlite`.
- [ ] `cargo test --workspace` continues to pass after the cleanup.

## Files to Modify/Create
- `state.sqlite` — Remove from git tracking (not necessarily from disk).
- `.gitignore` — Add `*.sqlite` entry to prevent future accidental commits of runtime database files.

## Dependencies
- Requires: Loop 1 (Dependencies, Key Parsing, and Constructor Refactor) — already completed
- Blocks: Project completion (this is the last acceptance QA blocker)
