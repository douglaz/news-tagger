---
artifact: qa-pass
loop: 1
iteration: 2
project: issue-3
backend: codex(gpt-5.3-codex-high)
role: qa
created_at: 2026-03-05T04:30:23Z
---

I’m validating this as a QA pass by building the workspace first, then manually exercising the Nostr path through real commands and finally running automated checks. I’ll gather concrete command outputs and map each acceptance criterion to explicit evidence.
I confirmed the tree is clean and found the task artifacts under `.ralph/projects/issue-3`. Next I’m reading `prompt.md`, the loop `spec`, and `impl-notes` to map each acceptance criterion to explicit manual tests.
I’ve finished reading the artifacts and I’m moving to execution now: first `cargo build` for a real artifact, then manual end-to-end library and CLI checks for key parsing/signing behavior, and then automated tests.
Build succeeded. Next I’m inspecting the Nostr adapter and CLI entrypoint to design manual commands that exercise invalid/valid key paths and cryptographic field consistency with real runtime behavior.
I confirmed the key implementation points in `nostr/mod.rs` and the CLI call-site update. I’m now running targeted manual checks using real invocations to validate invalid-key fail-fast behavior, hex vs `nsec` equivalence, and actual generated `pubkey/id/sig` consistency.
I’m creating a temporary manual harness in `/tmp` that imports the adapter crate and exercises it as a real consumer, including publishing to a local listener and cryptographically verifying captured event payloads. After that, I’ll run one CLI invocation to confirm the updated constructor path fails fast on invalid keys.
I have the API surface needed for a real consumer test. I’m now building a temporary executable in `/tmp` that imports `news-tagger-adapters`, exercises parsing/constructor failure paths, and performs a live publish to a local TCP listener so we can inspect and verify the emitted Nostr event fields.
The temporary harness hit a Rust lifetime issue in the invalid-input list, not a product issue. I’m fixing the harness and rerunning the same manual end-to-end checks.
I got strong manual evidence from the harness, but I still need explicit wrong-`HRP` and wrong-payload `nsec` cases plus a direct BIP-340 reference-vector check to satisfy every criterion concretely. I’m extending the temporary harness for those and rerunning.
I’m adding one real CLI-path check to confirm the constructor call-site wiring: I’ll run `news-tagger run` with Nostr enabled and an invalid key, then verify it fails immediately with the parsing error from `NostrPublisher::new`.
Manual runtime checks are in place and now I’m running automated regression commands as secondary confirmation: `cargo check --workspace`, targeted Nostr adapter tests, and full workspace tests.
# QA: PASS

## Manual Testing
- Built artifact:
  - Command: `cargo build --workspace`
  - Output: `Finished 'dev' profile ... target(s) in 2.16s`
- Manual library harness (real consumer binary in `/tmp/nostr_manual_qa`) exercising parsing, constructor fail-fast, live publish payload capture, ID recomputation, signature verification, JSON escaping, and BIP-340 vector:
  - Command: `cargo run --manifest-path /tmp/nostr_manual_qa/Cargo.toml`
  - Key output:
    - `hex_pubkey=6d6c...68e2`
    - `nsec_pubkey=6d6c...68e2` (equal)
    - Invalid matrix all rejected with clear errors:
      - empty -> `Nostr secret key is empty`
      - 63/65 hex -> `Invalid hex secret key length...`
      - non-hex -> `contains non-hex characters`
      - wrong HRP -> `expected 'nsec', got 'npub'`
      - 31/33 payload -> `expected 32 bytes, got 31/33`
      - zero scalar -> `Invalid Nostr secret key scalar...`
    - `new(trimmed_hex) => OK` (whitespace trimming works)
    - Live publish capture:
      - `raw_request_body=["EVENT","{\"id\":\"...\",\"pubkey\":\"...\",\"created_at\":...,\"kind\":1,\"tags\":[],\"content\":\"Manual QA content with \\\"quotes\\\", newline\\\\nbackslash\\\\ and unicode: café\",\"sig\":\"...\"}"]`
      - `event_id=...`
      - `recomputed_id=...` (equal)
      - `signature_verification=OK`
    - Reference vector:
      - `vector_pubkey_matches=true`
      - `vector_sig_matches=true`
- Manual CLI path check (updated constructor call-site exercised):
  - Command: `X_BEARER_TOKEN=dummy NOSTR_NSEC=zzz cargo run -p news-tagger -- run --once --config /tmp/qa_nostr_config_invalid.toml`
  - Output: `Error: Invalid hex secret key length: expected 64 characters, got 3`
  - Backtrace includes:
    - `news_tagger_adapters::nostr::NostrPublisher::new ... crates/adapters/src/nostr/mod.rs:125`
    - `news_tagger::commands::run::build_nostr_publisher ... crates/cli/src/commands/run.rs:233`

## Automated Tests
- `cargo check --workspace`: passed.
- `cargo test -p news-tagger-adapters nostr -- --nocapture`: passed (`11 passed, 0 failed`).
- `cargo test --workspace`: passed (all workspace tests green; adapters `47 passed`, domain `14 passed`, CLI tests `3 passed`).

## Acceptance Criteria Verification
- [x] Workspace deps added (`k256` + `bech32`): verified via `rg -n "^k256\\s*=|^bech32\\s*=" Cargo.toml crates/adapters/Cargo.toml` (root has `k256 = { version = "0.13", features = ["schnorr"] }`, `bech32 = "0.11"`).
- [x] Adapter deps wired as workspace deps: same `rg` output shows `crates/adapters/Cargo.toml` has both.
- [x] `parse_secret_key(raw: &str) -> Result<SigningKey>` behavior: manual harness proved trim, hex parsing, `nsec` decode, HRP enforcement, payload length enforcement, scalar rejection, and clear errors.
- [x] `NostrPublisher` stores `signing_key: Option<SigningKey>` and module no longer uses secrecy: source check + `rg -n "use secrecy|SecretString|ExposeSecret" crates/adapters/src/nostr/mod.rs crates/cli/src/commands/run.rs` returns only CLI usage.
- [x] `NostrPublisher::new` fail-fast invalid keys: manual harness invalid matrix and CLI run both fail immediately with parsing errors.
- [x] `NostrPublisher::disabled()` remains infallible with disabled behavior: `nostr::tests::test_disabled_publisher` passes and runtime behavior is preserved.
- [x] CLI call-site updated to direct constructor result: `sed -n '224,236p' crates/cli/src/commands/run.rs` shows `NostrPublisher::new(secret_key.expose_secret(), config.nostr.relays.clone())`.
- [x] Hex and `nsec` forms yield same key/pubkey: manual harness printed equal pubkeys for both forms.
- [x] Unit tests cover parsing/equivalence/invalid matrix: confirmed by executed Nostr test names (`test_parse_secret_key_hex`, `test_parse_secret_key_nsec`, `test_parse_secret_key_hex_and_nsec_equivalence`, `test_parse_secret_key_invalid_matrix`).
- [x] Project compiles: `cargo check --workspace` passed.
- [x] Existing tests compile/pass after constructor refactor: `cargo test --workspace` passed.

- [x] Prompt AC: fixed-input cryptographic consistency (`pubkey`, `id`, `sig`) validated manually via captured publish payload, recomputed ID match, and Schnorr verification.
- [x] Prompt AC: JSON escaping edge case validated manually (quotes/newline/backslash/unicode content in payload; recomputed ID matched).
- [x] Prompt AC: deterministic BIP-340 reference vector validated manually (`vector_pubkey_matches=true`, `vector_sig_matches=true`).
